#include <stdio.h>
void main(){
    int x=5;
    printf("Memory address of %d is %p\n", x, &x);
    printf("Here the random numbers after the value of variabe is the memory address of that variable called pointer specified by Percenatge p");
}